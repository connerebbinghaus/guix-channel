(define-module (conner packages swtpm)
    #:use-module (guix deprecation))

(define-deprecated/public-alias swtpm (@ (gnu packages virtualization) swtpm))
(define-deprecated/public-alias libtpms (@ (gnu packages virtualization) libtpms))


   
   
	    
		  
   
